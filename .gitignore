# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
apps/*/dist/
build/
out/
.next/

# Cache
.cache/
.turbo/
.eslintcache
*.tsbuildinfo
.vite/

# Environment
.env
.env.*
!.env.example
!apps/*/.env.example
!apps/*/.env.production.example
!apps/*/.env.local.example

# Runtime logs
logs/

# Local databases
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal

# Temporary files (e.g. Prisma engine extraction)
*.tmp
*.tmp[0-9]*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Deploy
.vercel
.netlify

# Lock files alternativos (manter apenas um)
yarn.lock
pnpm-lock.yaml

# Vite output logs
vite-output.log

.claude/

# Artefatos de trabalho local (não versionar)
tmp/
output/
instrucoesAI/
apps/backend/uploads/
produtos-modelos-questionarios.json
operacao-tarefas-legado.json
*elementor*.html

# Dumps de banco (nunca versionar)
*.sql
!apps/backend/prisma/migrations/**/*.sql
!apps/backend/prisma/legacy/migrations/**/*.sql
# SQL estático versionado (não é dump) usado pelo workflow de reconciliação de baseline
!.github/workflows/qa-catalog2-seed-rows.sql

# Cliente Prisma gerado do banco legado (regerado via `npm run db:legacy:generate`)
apps/backend/src/legacy/generated/

# Links de primeiro acesso gerados para a migração — tratar como senha
links-primeiro-acesso*.csv
